{
  "name": "rsf-session-logger",
  "version": "1.0.0",
  "private": true,
  "description": "Session and attendance logger for Rochester School of Fitness — Android tablet app",
  "scripts": {
    "serve": "python3 -m http.server 8777 --directory www",
    "add:android": "cap add android",
    "sync": "cap sync android",
    "build:debug": "cap sync android && cd android && ./gradlew assembleDebug",
    "build:release": "cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/filesystem": "^6.0.1",
    "@capacitor-community/speech-recognition": "^6.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
